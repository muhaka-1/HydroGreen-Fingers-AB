#include "wifi_manager.h"
#include "esp_log.h"

static const char *TAG = "WIFI_MANAGER";

esp_err_t wifi_manager_init(void) {
    // TODO: IoT Engineer implementerar Wi-Fi STA konfiguration här
    ESP_LOGI(TAG, "wifi_manager_init() anropad (Placeholder för IoT Engineer)");
    return ESP_OK;
}
